import React from 'react'
import { _NAMESPACE_ } from "..";
import { TConfigProps } from '../useConfig';
import UploadConfig from './UploadConfig'

export default (props: TConfigProps) => {
  return <UploadConfig {...props} />
}
